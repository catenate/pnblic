cp -r /home/jdc/git/catenate.github.com/css .
jekyll --server
|fmt -w 62|sed 's,$,  ,'
|sed 's,^,	,'
|sort|uniq

git status
git commit -a -m 'add disqus comments'
git push origin gh-pages
git checkout master
